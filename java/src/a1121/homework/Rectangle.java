package a1121.homework;

public class Rectangle {

    // 필드 선언
    int width = 0; // 초기값 0
    int height = 0; // 초기값 0


    // 생성자 선언

    Ractangle() {};

    Ractangle(int width) {
        this.width = width;
        this.height = height;
    };

    Ractangle(int width, int height) {
        this.width = width;
        this.height = height;
    };
}
